# Cursor 設定

このフォルダには Cursor IDE のプロジェクト設定を格納します。

## フォルダ構成

- `rules/` - AI ルール（`.mdc` ファイル）
  - プロジェクト固有のコーディング規約
  - ファイルパターンに応じたルール
  - 常時適用するルール

## ルールの追加

1. `rules/` に `.mdc` ファイルを作成
2. YAML frontmatter で適用条件を指定
3. マークダウンでルール内容を記述
